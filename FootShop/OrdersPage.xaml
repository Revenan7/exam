<Page x:Class="FootShop.OrdersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FootShop"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Заказы"
      FontFamily="Times New Roman">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0"
                   Text="Заказы"
                   FontSize="26"
                   FontWeight="Bold"
                   Margin="0 20 0 10"
                   HorizontalAlignment="Center"/>

        <!-- Панель администратора -->
        <StackPanel Grid.Row="1"
                    x:Name="AdminPanel"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="10">

            <Button Content="Добавить заказ"
                    Width="140"
                    Margin="5"
                    Click="Add_Click"/>

            <Button Content="Редактировать"
                    Width="140"
                    Margin="5"
                    Click="Edit_Click"/>

            <Button Content="Удалить"
                    Width="140"
                    Margin="5"
                    Background="#FF6347"
                    Click="Delete_Click"/>
        </StackPanel>

        <!-- СПИСОК ЗАКАЗОВ -->
        <ListView x:Name="OrdersList"
                  Grid.Row="2"
                  Margin="10"
                  HorizontalContentAlignment="Stretch"
                  MouseDoubleClick="OrdersList_MouseDoubleClick">

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderThickness="1"
                            BorderBrush="Black"
                            Margin="5"
                            Padding="10"
                            HorizontalAlignment="Stretch">

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!-- Левая часть -->
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding OrderID}"
                                           FontWeight="Bold"
                                           FontSize="16"/>

                                <TextBlock Text="{Binding StatusName}"
                                           Margin="0,5,0,0"/>

                                <TextBlock Text="{Binding AddressName}"
                                           TextWrapping="Wrap"/>

                                <TextBlock Text="{Binding OrderDate,
                                          StringFormat='Дата заказа: {0:d}'}"/>
                            </StackPanel>

                            <!-- Правая часть -->
                            <TextBlock Grid.Column="1"
                                       Text="{Binding DeliveryDate,
                                       StringFormat='Дата доставки: {0:d}'}"
                                       FontWeight="Bold"
                                       VerticalAlignment="Top"
                                       Margin="20,0,0,0"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
